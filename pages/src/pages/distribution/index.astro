---
import Container from "@moesegfault/style/astro/Container.astro";
import CodeBlock from "../../components/CodeBlock.astro";
import DocsLayout from "../../layouts/DocsLayout.astro";

const root = "https://style.moesegfault.dev";
---

<DocsLayout
  title="版本资源"
  description="MoeSegFault Style 的版本化静态资源路径和更新策略。"
  currentPath="/distribution/"
>
  <header class="page-intro">
    <Container>
      <p class="kicker">DISTRIBUTION</p>
      <h1>选一个地址，直接开始用。</h1>
      <p class="lede">不方便安装 npm 包也没关系：复制固定版本地址即可稳定使用，或者选择 latest 自动跟随最新稳定版。</p>
    </Container>
  </header>

  <section class="section section-tint" aria-labelledby="paths-title">
    <Container>
      <div class="section-heading">
        <div><p class="eyebrow">Asset map</p><h2 id="paths-title">可直接使用的资源</h2></div>
      </div>
      <div class="table-scroll">
        <table>
          <thead><tr><th>资源</th><th>固定版本 URL</th><th>什么时候使用</th></tr></thead>
          <tbody>
            <tr><td>全量样式</td><td><code>{root}/v0.1.1/css/all.css</code></td><td>最省事：一次引入完整视觉样式</td></tr>
            <tr><td>设计令牌</td><td><code>{root}/v0.1.1/css/tokens.css</code></td><td>只使用颜色、间距、圆角等 CSS 变量</td></tr>
            <tr><td>基础样式</td><td><code>{root}/v0.1.1/css/foundation.css</code></td><td>需要主题和基础排版，不需要组件外观</td></tr>
            <tr><td>组件样式</td><td><code>{root}/v0.1.1/css/components.css</code></td><td>已经引入基础样式，只补充组件外观</td></tr>
            <tr><td>Token JSON</td><td><code>{root}/v0.1.1/tokens/tokens.json</code></td><td>在应用或构建脚本中读取解析后的令牌</td></tr>
            <tr><td>DTCG JSON</td><td><code>{root}/v0.1.1/tokens/tokens.dtcg.json</code></td><td>导入支持 DTCG 的设计或转换工具</td></tr>
          </tbody>
        </table>
      </div>
    </Container>
  </section>

  <section class="section" aria-labelledby="strategy-title">
    <Container>
      <div class="section-heading">
        <div><p class="eyebrow">Choose a path</p><h2 id="strategy-title">你希望它如何更新？</h2></div>
      </div>
      <div class="asset-grid">
        <article class="asset-card">
          <h3>推荐：锁定一个版本</h3>
          <p><code>/v0.1.1/…</code> 的内容不会改变，页面也不会因为库发布新版而突然变化。准备升级时，只需更换 URL 中的版本号。</p>
          <CodeBlock label="HTML" code={`<link rel="stylesheet"\n  href="${root}/v0.1.1/css/all.css">`} />
        </article>
        <article class="asset-card">
          <h3>只获取最新基础资源</h3>
          <p><code>/css</code>、<code>/tokens</code> 和 <code>/colors</code> 会自动跟随最新稳定版。颜色页面位于 <code>/colors</code>，CSS 和 JSON 可直接使用对应文件地址。</p>
          <CodeBlock label="HTML" code={`<link rel="stylesheet"\n  href="${root}/css/all.css">`} />
        </article>
        <article class="asset-card">
          <h3>自动使用最新完整版本</h3>
          <p><code>/latest/…</code> 始终跟随最新稳定版，适合示例、个人页面和愿意自动接收更新的项目；重要应用更建议锁定固定版本。</p>
          <CodeBlock label="HTML" code={`<link rel="stylesheet"\n  href="${root}/latest/css/all.css">`} />
        </article>
      </div>
      <div class="callout">
        <h3>不知道怎么选？</h3>
        <p>
          正式项目优先使用 <code>/v0.1.1/…</code>；原型和展示页可以使用 <code>/latest/…</code>。
          浏览器和边缘缓存可能让 latest 的更新稍晚出现，这不会影响固定版本地址。
        </p>
      </div>
    </Container>
  </section>

  <section class="section section-tint" aria-labelledby="release-title">
    <Container>
      <div class="section-heading">
        <div><p class="eyebrow">Versions</p><h2 id="release-title">查看版本与升级</h2></div>
      </div>
      <div class="feature-grid">
        <article class="feature-card"><span class="feature-number">01</span><h3>查看所有版本</h3><p>打开根目录的 <code>/manifest.json</code>，即可找到所有可用版本及其清单地址。</p></article>
        <article class="feature-card"><span class="feature-number">02</span><h3>按需升级</h3><p>确认新版本符合需要后，把 URL 中的版本号替换为目标版本即可。</p></article>
        <article class="feature-card"><span class="feature-number">03</span><h3>旧版本继续可用</h3><p>每个精确版本都会独立保留；暂时不升级的页面可以继续使用原地址。</p></article>
      </div>
    </Container>
  </section>
</DocsLayout>
